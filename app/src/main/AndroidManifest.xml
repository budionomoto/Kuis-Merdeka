<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.xyzsystem.budiono.kuismerdeka">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActSatu">
        </activity>
        <activity android:name=".ActDua" />
        <activity android:name=".ActTiga" />
        <activity android:name=".ActEmpat" />
        <activity android:name=".ActLima">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".ActEnam" />
        <activity android:name=".ActTujuh" />
    </application>

</manifest>